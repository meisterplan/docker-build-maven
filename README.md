# maven Docker Image

A minimal docker image that provides a docker image with maven, make and openjdk

You can find the Docker image on DockerHub [here](https://hub.docker.com/repository/docker/meisterplan/build-maven).

## Currently Available Tags

| Image                   | Tag     | Description                              |
| ----------------------- | ------- | ---------------------------------------- |
| meisterplan/build-maven | latest  | Updated on every push to `latest` branch |
| meisterplan/build-maven | `<TAG>` | Updated on every tag creation            |
